# CourtPass root .htaccess
# Only the public/ folder is exposed to the browser.
# Everything else (app, config, database, legacy, tests) stays private.
Options -Indexes
RewriteEngine On

RewriteRule ^$ public/ [L]
RewriteCond %{REQUEST_URI} !/public/
RewriteRule ^(.*)$ public/$1 [L]
